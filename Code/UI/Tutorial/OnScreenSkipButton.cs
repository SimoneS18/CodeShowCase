using UnityEngine;
using UnityEngine.UI;

namespace UI.Tutorial
{
public class OnScreenSkipButton : MonoBehaviour
{
    public Button Skip;

    // Start is called before the first frame update
    private void Start() {}

    // Update is called once per frame
    private void Update() {}
}
}